# Hy3 Research Assistant 安装说明

## 一、主项目安装（Web 应用）

### 快速安装（推荐）

```bash
# 1. 进入项目目录
cd hy3-research-assistant-package

# 2. pip 安装（自动处理所有依赖）
pip install -e .

# 3. 配置 API 密钥
# 复制 .env.example 为 .env，然后编辑填入你的 API Key

# 4. 启动
hy3-research
# 浏览器访问 http://localhost:8000
```

### 手动安装

```bash
# 1. 配置 API 密钥
copy .env.example .env
# 编辑 .env 填入 HY3_API_KEY

# 2. 安装依赖
cd backend
pip install -r requirements.txt

# 3. 启动
python main.py
```

### Windows 一键启动

直接双击 `start.bat`（自动检测 API Key、安装依赖、启动服务）。

---

## 二、MCP Server 安装

将 Hy3 能力接入 Claude Desktop / Cursor / CodeBuddy。

### 安装

```bash
cd hy3-mcp-server
pip install -e .
```

### Windows 一键安装

双击 `hy3-mcp-server/setup.bat`

### 配置 Claude Desktop

在 `claude_desktop_config.json` 中添加：

```json
{
  "mcpServers": {
    "hy3": {
      "command": "hy3-mcp",
      "env": {
        "HY3_API_KEY": "你的API密钥"
      }
    }
  }
}
```

### MCP Server 提供的工具

| 工具名 | 功能 |
|--------|------|
| hy3_research | 深度研究：搜索 + 分析 + 报告 |
| hy3_code_review | 代码评审：Bug检测 + 性能 + 安全 |
| hy3_doc_qa | 文档问答：解析 + 精准回答 |
| hy3_data_analyze | 数据分析：CSV/JSON + 洞察 |
| hy3_chat | 通用对话 |

---

## 依赖关系图

```
hy3-research-assistant (主项目)
├── FastAPI + Uvicorn           ← Web 框架
├── OpenAI SDK                  ← Hy3 API 调用
├── httpx + BeautifulSoup       ← 网页搜索
├── PyPDF2 + python-docx        ← 文档解析
└── python-multipart + aiofiles ← 文件上传

hy3-mcp-server (MCP Server)
├── fastmcp                     ← MCP 协议
├── OpenAI SDK                  ← Hy3 API 调用
├── httpx                       ← HTTP 请求
└── python-dotenv               ← 环境变量
```

两个包可独立安装使用，互不依赖。
